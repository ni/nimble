import { OkExButton } from '@ni/ok-react/ex/button';
import { SubContainer } from '../SubContainer';

export function ExButtonSection(): React.JSX.Element {
    return (
        <SubContainer label="Ex Button (Ok)">
            <OkExButton>Ok</OkExButton>
        </SubContainer>
    );
}
