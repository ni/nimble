/**
 * The browser script loaded and run in the browser context by the karma plugins
 */

// eslint-disable-next-line import/no-useless-path-segments
import { applyExtensions } from './apply-extensions/index.js';

applyExtensions();
