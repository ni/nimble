import { tabsToolbarTag } from '@ni/nimble-components/dist/esm/tabs-toolbar';

export const defaultSlotDescription = `Slot to display elements on the left side of the \`${tabsToolbarTag}\`. Elements that do not specify a slot will be placed in this slot.`;
export const endSlotDescription = `Slot to display elements on the right side of the \`${tabsToolbarTag}\`. If slotted content is only in the \`end\` slot, the separator will be hidden.`;
