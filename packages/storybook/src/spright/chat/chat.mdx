import { Controls, Canvas, Meta, Title } from '@storybook/blocks';
import * as chatStories from './chat.stories';
import ComponentApisLink from '../../docs/component-apis-link.mdx';
import { chatTextBubbleTag } from '../../../../spright-components/src/chat-text-bubble';

<Meta of={chatStories} />
<Title of={chatStories} />

Chat components are used to conduct and display a conversation with a chat bot or other users.
The components can collect user input and display responses; the application is responsible 
for connecting them to a source of data.

Components include low level chat bubbles and input fields and higher level composite components
representing an entire conversation or chat window.

<Canvas of={chatStories.chatTextBubble} />

## API

TODO higher level window example

<ComponentApisLink />

### Chat Text Bubble

Use the <Tag name={chatTextBubbleTag} /> element to display text in a cute little bubble.

<Canvas of={chatStories.chatTextBubble} />
<Controls of={chatStories.chatTextBubble} />

{/* ## Usage */}

{/* ## Accessibility */}

{/* ## Resources */}