import { Controls, Canvas, Meta, Title } from '@storybook/blocks';
import * as rectangleStories from './rectangle.stories';

<Meta of={rectangleStories} />
<Title of={rectangleStories} />

A rectangle is a temporary, demo Spright component.

<Canvas of={rectangleStories.rectangle} />
<Controls of={rectangleStories.rectangle} />

## Usage

Don't.
