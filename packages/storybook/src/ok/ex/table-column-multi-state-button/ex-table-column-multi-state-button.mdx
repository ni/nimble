import { Controls, Canvas, Meta, Title } from '@storybook/addon-docs/blocks';
import * as exTableColumnStories from './ex-table-column-multi-state-button.stories';
import ComponentApisLink from '../../../docs/component-apis-link.mdx';

<Meta of={exTableColumnStories} />
<Title of={exTableColumnStories} />

An Ex Multi-State Button Table Column is meant to serve as an example of a custom Nimble
Table Column outside of the Nimble package (e.g. in `ok-components`).

<Canvas of={exTableColumnStories.exTableColumnMultiStateButton} />

## API

<Controls of={exTableColumnStories.exTableColumnMultiStateButton} />
<ComponentApisLink />

## Usage

Not for direct use.
