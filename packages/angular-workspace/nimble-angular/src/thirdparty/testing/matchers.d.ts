// Manually created for Nimble. Matchers interface should include any functions defined in the object being passed to jasmine.addMatchers() in ./matchers.ts.
declare namespace jasmine {
    interface Matchers<T> {
        toHaveText(expected: string): boolean;
    }
}