/**
 * Metadata for multi-color icons.
 * This list is used by build scripts in nimble-components, angular-workspace,
 * and react-workspace to identify which icons should not be auto-generated.
 */

/**
 * List of icon names (in spinal-case) that are multi-color icons.
 * Multi-color icons are manually created in nimble-components/src/icons-multicolor/
 * and use multiple theme colors instead of a single severity-based color.
 * @type {ReadonlyArray<string>}
 */
export const multiColorIcons = ['circle-partial-broken'];
