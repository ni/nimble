import { DocsStory, Meta, Controls, Title } from '@storybook/blocks';
import { NimbleCard } from './card.react';
import * as cardStories from './card.stories';

<Meta of={cardStories} />
<Title of={cardStories} />

The \`nimble-card\` is a container that is designed to contain arbitrary content that is specified by a client
application. The \`nimble-card\` is intended for grouping related content.

<DocsStory of={cardStories.card} expanded={false} />
<Controls of={cardStories.card} />

## Appearances

## Appearance Variants

## Usage

## Examples

## Accessibility

## Resources
