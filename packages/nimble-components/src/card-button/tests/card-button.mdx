import { DocsStory, Meta, Controls, Title } from '@storybook/blocks';
import { NimbleCardButton } from './card-button.react';
import { buttonTag } from '../../button';
import * as cardButtonStories from './card-button.stories';

<Meta of={cardButtonStories} />
<Title of={cardButtonStories} />

The \`nimble-card-button\` is a button that is designed to contain arbitrary content that is specified by a client
application. The \`nimble-card-button\` is intended to be larger and more prominent on a page than the standard \`${buttonTag}\`.

Note: The styling for the "Color" theme is not complete.

<DocsStory of={cardButtonStories.cardButton} expanded={false} />
<Controls of={cardButtonStories.cardButton} />

## Appearances

## Appearance Variants

## Usage

## Examples

## Accessibility

## Resources
