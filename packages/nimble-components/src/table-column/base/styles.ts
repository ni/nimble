import { css } from '@microsoft/fast-element';

export const styles = css`
    :host {
        display: contents;
    }

    .header-content {
        display: contents;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
`;
