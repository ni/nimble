import {
    Controls,
    DocsStory,
    Meta,
    Stories,
    Title,
    Description
} from '@storybook/blocks';
import * as radioGroupStories from './radio-group.stories';

<Meta of={radioGroupStories} />

<Title of={radioGroupStories} />
<Description of={radioGroupStories} />

<DocsStory of={radioGroupStories.radioGroup} expanded={false} />
<Controls of={radioGroupStories.radioGroup} />

# Usage Docs

## Angular Usage

The Angular CVA for the radio button group ignores the value of `callSetDisabledState` configured on the form module. Instead, it always uses the default value of `always`.
