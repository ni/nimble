module.exports = {
    // Beachball manages versioning and tagging of the npm package but not publishing to npm.
    // Angular packages are built into the dist/ folder and need to be published from there instead.
    publish: false
};
