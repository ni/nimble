import { NimbleSelectDirective } from '../nimble-select.directive';

describe('NimbleSelectDirective', () => {
    it('should create an instance', () => {
        const directive = new NimbleSelectDirective();
        expect(directive).toBeTruthy();
    });
});
